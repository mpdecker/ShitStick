name: shit_covered_stick
description: "A dried shit stick."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^18.0.0
  timezone: ^0.9.0
  shared_preferences: ^2.3.0
  workmanager: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
